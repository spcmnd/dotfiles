# spcmnd's dotfiles

## Tmux

Basic configuration.

## Aerospace (macOS)

Configuration to support the QWERTZ keyboard layout. It needs Raycast (or similar software that support hyper key) to have the Hyper Key setup to be combination of CTRL+ALT(OPTION)+CMD in Caps Lock for example.

Custom scripts are located in `/scripts`.
